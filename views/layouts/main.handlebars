<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" href="/public/css/style.css">
</head>
<body>
  <div class= "navbar">
    <header>
      <h1>
        <a href="/">home</a>
      </h1>
      {{!-- conneting to login and logout fuction --}}
      <nav>
        {{#if logged_in}}
        <a href="/dashboard">Dashboard</a>
        <button class="logout" id="logout">Logout</button>
        {{else}}
        <a href="/login">Login</a>
        {{/if}}
      </nav>
    </header>
    {{!-- rendering the title of the website on page --}}
       <div class = "title">
        <div class = "title-name">
           <h1>Tech Blog</h1>
          </div>
        </div>
  </div>
   {{!-- main body handlebars --}}
  <main> 
    {{{ body }}}
  </main>
 {{!-- calling on logout JS --}}
  {{#if logged_in}}
  <script src="/public/js/logout.js"></script>
  {{/if}}
</body>
</html>
